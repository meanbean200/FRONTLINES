> Superseded by the September 23 deterministic-garrison brief. This is historical evidence, not current certification. See [current acceptance](deterministic-garrisons.md). The study is stopped; no neural model was adopted or installed.

The `living-3-final` run stopped at 01:18:20 on September 23 after a 300-person soak exposed 191 deaths despite stock availability. Its learned-101 export, partial learned-202 checkpoint, stop receipt and source snapshot remain preserved. No held-out living-3 comparison or full matched replay set completed. The following budget/progress narrative is a historical plan, not an active job.

# Local study boundary

The first pilot artifact was created September 22, 2026 at 23:00 local time. The initial `living-1` study was stopped after three completed learned seeds when implementation review found missing rationing and delivery observations. Its checkpoints and source snapshot remain under `study-runs/first-study`; no results are pooled with the revised rules.

`living-2-smoke` caught a source-identity change during a TypeScript correction and failed closed. `living-2-study` was stopped during preflight for final morale/relief behavior changes. Those artifacts are retained too.

The `living-2-final` comparison began at 23:38 and was stopped at 00:38 on September 23 after held-out scenarios exposed avoidable deaths. Six trained exports and the partial evaluations remain preserved, but are not adopted or pooled with revised rules. Diagnostics found surplus alert posts persisting after stand-down, exhausted helpers interrupting their own recovery, and successive relief positions drifting toward branch ends. The old test seeds became development regressions once used to repair those defects.

The `living-3` regression sequence preserves all three attempts under `output/living-3-preflight-*.json`. The first two still had deaths; the third completed all twelve 72-hour cases with zero deaths. Critical-need time was not zero, so this is a regression check, not a claim of ideal routines.

The fresh `living-3-final` comparison starts before 01:00 with previously unused evaluation seeds 19901 through 19912. It has a **five-hour hard wall-clock cap**, a per-training-run one-hour cap, a 30-minute pilot cap and a 90-minute evaluation cap. Its deadline remains before the original eight-hour ceiling of approximately 07:00 local. Training reserves evaluation time and cannot consume the complete five-hour cap. Earlier failed work is included in that original ceiling, not reset by the new directory or rules version.

Each new run requests 131,072 transitions and may finish before its one-hour cap. Consequently this is an exploratory short-budget comparison, not a claim of six complete hours of training. Insufficient training remains an inconclusive outcome. There is no recurring job, automatic extension, cloud execution or paid service.

The exact configuration is in `report.json` and `source-manifest.json`. The watchdog terminates its owned Node hosts at the deadline. The model is never adopted automatically; the default coordinator is rule-based.
