# FRONTLINES overnight development — 23 September 2026

## Authority and stop boundary

The user asked for local improvements while asleep: better visuals, menus, playable complexity, towns, short modes, winning, consideration of combat, and optional AI training. This is an eight-hour local continuation, ending **2026-09-23 21:45 UTC / 16:45 America/Chicago**, or earlier on user direction. Heartbeat `frontlines-overnight-development` runs in this task every 30 minutes. Check the deadline before each slice and long experiment; pause the heartbeat at the deadline. No cloud spending, publishing, purchases, destructive cleanup, new tasks, or subagents. Preserve saves and failed evidence.

## Design promise

A legible miniature countryside battlefield: easy squad-level orders with genuine trench, needs, and supply systems underneath. Muted moss, warm plaster, slate roofs, cream typography; friendly teal, enemy rust, objective amber. Clear silhouettes and restrained effects, not texture noise. DOM menus and a compact objective HUD leave the center of the Three.js playfield open. Menus pause the simulation and gate world input.

## Ordered sprint

**Latest user direction, 14:18 UTC:** restrict further work to looking for bugs, playing through the existing modes, and small polish. The existing heartbeat has been updated to match. Do not begin neural training or expand major systems in this pass; the earlier sprint below is historical context. Existing stop deadline remains unchanged.

1. Implement a start/pause/after-action flow and finite operations alongside the existing peaceful sandbox. Starting fresh must not overwrite saved campaigns.
2. Make nearby settlements meaningful capture/hold objectives with finite physical supply caches. Short-session win/loss rules must run headlessly and survive save/load.
3. Contain first combat to operations: deterministic rifle fire, line of sight, cover, finite ammunition, suppression, basic enemy orders, readable friendly/enemy states. No unsupported promises about machine guns, armor, destruction, or trained combat intelligence.
4. Improve village/world and UI readability. Real-browser QA of starting, commanding, pausing, resuming, saving/loading, and outcomes; check smaller viewport and errors.
5. Stabilize: full regression suite, build, operation scenarios and performance. Retain living-trench tests and prior evidence; investigate poor default experience and mature large-camp cost as time allows.
6. Only after a stable rules baseline: optional newly versioned, bounded neural pilot. Do not mix old incompatible checkpoints/results or silently replace deterministic policies. If no valid comparison fits the window, explicitly report training not adopted/inconclusive.

## Baseline (from preceding completed work; reverify after changes)

78 tests / 8 files and production build passed. Living-garrison 300-soldier 72-hour soak had no deaths, balanced inventory and no final travel stalls. Mature 1,000-person performance remains unresolved; fresh 1,000-person browser results are not mature-camp proof. Unassigned reserves can exhaust finite supplies over long sandbox sessions. No neural model is adopted. See deterministic-garrisons.md and deterministic-performance.md for exact scope and retained failures.

## Current status

- 13:47 UTC: heartbeat created and active.
- 14:14 UTC: **first playable operations slice implemented and verified**, not a finished game. Menus, 10/15-minute operations, objectives, finite town caches, rifle combat, simple enemy firing lines, after-action results, village detail and a 28-person fully assigned default sandbox are in. Old fixtures and saves are preserved. See `operations-verification.md` for exact tests, artifacts, failures and claim limits.
- Verification: 93 tests / 9 files passed with two workers; build passed (`index-DOiozKSj.js`). An unrestricted parallel test run had one 5-second timeout, then passed isolated and with capped concurrency; no assertions relaxed. Fresh default sandbox 72-hour soak: all 28 active, zero deaths/critical-need hours, ledger error below 1.7e-11. Actual browser offensive won at 388.25 sim seconds with 42/48 able. A 20-second production firefight at 5× measured p95 6.2 ms / worst 6.3 ms and full simulation advance. These are scoped results, not large-camp certification or subjective acceptance.
- Neural training: **not started**. Critical: current `GarrisonSystem.coordinate()` unconditionally calls the rule policy; old study fields are inactive. A new experimental study must first prove that policy output actually influences duties, version the simulation/reward/observation, retain deterministic normal play, and pass constant/shuffled action checks. No adopted neural model.
- Next run priority under the **narrowed bug/polish scope**: long combat-garrison playtests, repeated mode/save/quality changes, interrupted emergencies and crowded/aged camps. Do not add major systems or start neural training. See the newer `bugpass-2026-09-23.md` for current evidence and remaining limits.
- Keep the live Vite server on port 4173. QA uses a separate Edge profile. Do not overwrite existing evidence files; choose increasing revision suffixes. Recheck all process IDs before stopping only owned QA helpers.
- 14:18 UTC: final `npm test` passed all 93 tests in 19.67 seconds. Closed the owned `frontlines-overnight` browser session and stopped only the verified QA preview process on port 4174. The user-facing server on port 4173 still responds HTTP 200. Heartbeat remains active for the next slice.
- 14:39 UTC: completed the user's bug/playthrough pass. **105 tests / 13 files pass**, build `index-Ce3k-Vg9.js`. Reproduced and fixed survivor-center movement stalls, keyboard focus/Escape, post-victory orders, sleeping garrisons ignoring fire, hidden load failures, small-window overlap and unsafe graphics-loss advancement. Profiled 50-person assignment improved from 2,439.6 ms to 12.2 ms through fully checked direct approaches, not weaker collision rules. Real UI defense win at 388.3 seconds / 21 able; offensive win at 363.3 seconds / 43 able. Final recovery test paused exactly and restored without new warnings/errors. Fresh 72-hour 28-person soak: no deaths/critical-need hours, balanced stock. Full evidence, source identities and failed probes: `bugpass-2026-09-23.md`.
- 14:40 UTC cleanup: closed only QA browser `frontlines-bugpass` and verified preview process 32372 on port 4174. User-facing Vite process 35696 on port 4173 remains running and returned HTTP 200. No training running. Heartbeat remains narrowed to bugs/playthrough/polish, with the original stop deadline.
- 14:55 UTC heartbeat follow-up: **112 tests / 15 files pass**, production build `index-D_jcRfRK.js`, rules `deterministic-3`. Fixed bypassable/multiple supply-emergency pauses, combat casualty duty/capacity reservations, and unsynchronized rendering settings; clarified defense result stats. Real garrison defense won at 418.1 seconds with 27/48 able (pre-fix playthrough that exposed casualty issues). Final production browser verified multi-emergency decisions through save/load/help/menu, all three mode switches without losing the saved battle, and correct living network occupancy. See `bugpass-followup-2026-09-23.md` for fixtures, screenshots, identities, retained failures, and limits. No training or major systems added. Next scoped checks: physical emergency recovery trips, async route/load transitions, aged crowd profiling, and inspector text wrapping.
- 14:56 UTC cleanup: closed only isolated QA browser `frontlines-followup` and stopped verified preview process 17000 on port 4174. The user-facing Vite process 35696 remains on port 4173 and returned HTTP 200. Heartbeat stays active within the original 21:45 UTC deadline; no training is running.
- 15:11 UTC heartbeat slice: **122 tests / 17 files pass**, production `index-CIxL0Zn_.js`, rules `deterministic-4`. Fixed late route replies mutating frozen/replaced worlds, pending saved movement not restarting in a fresh simulation, drawing/input state surviving Load, and narrow garrison-inspector overflow. Browser physical recovery and withdrawal succeeded through save/load with exact accounting in explicitly synthetic shortage fixtures. Six operation checks preserve prior outcomes. See `recovery-load-bugpass-2026-09-23.md` for evidence and limitations. Next scoped priorities: unreachable recovery targets and aged crowded-camp profiling; no training or new systems.
- 15:24 UTC cleanup: closed owned isolated QA browser `frontlines-recovery` and stopped verified preview PID 3160 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Heartbeat stays active until the original 21:45 UTC deadline, with no training running.
- 15:41 UTC heartbeat slice: **131 tests / 19 files pass**, build `index-B0wrO2dJ.js`, rules `deterministic-5`. Fixed recovery assignments into disconnected trenches, nearest-first reachable target selection, exact physical endpoints, and old empty blocked pickups. Browser reproduced a stuck saved carrier and verified his recovery in the final build. CPU profiling found a full-edge membership scan in route searches; an exact local spatial index cut a matched continuation from 23.1 s to 2.2 s with identical state. Aged 300-person browser checks pass at 1×/5× (p95 6.2 ms, full advancement); aged 1,000-person 5× **still fails** (41.25 simulation seconds in 21.41 wall seconds, p95 163.9 ms). Older fixture provenance and all failed evidence retained. See `recovery-targets-bugpass-2026-09-23.md`. Next: bounded external-route profiling and revising acknowledged emergency choices. No training/new systems.
- 15:57 UTC cleanup: closed owned QA session `frontlines-targets` and stopped verified preview PID 20788 on port 4174. User Vite PID 35696 on port 4173 remains healthy (HTTP 200). No training is running; heartbeat remains active until the original deadline.
- 16:11 UTC heartbeat slice: **142 tests / 20 files pass**, build `index-BCWLjDLW.js`, rules `deterministic-6`. Added contextual review of acknowledged supply decisions with exact pause/speed and save continuity. Real browser withdrawal exposed early arrivals hoarding reserve rations; fixed with needed-only half-rations and timed consumption. Final repeat fed all 16 arrivals with conserved inventory. Bounded mature-camp tracing found repeated failed loaded-carrier return searches; no routing-performance fix or new certification claimed. See `emergency-review-bugpass-2026-09-23.md`. Next: withdrawal stock/cover HUD mismatch and failed return-route reproduction. No training or new systems.
- 16:24 UTC cleanup: closed owned QA browser `frontlines-decisions` and stopped verified preview PID 22536 on port 4174. User Vite PID 35696 on port 4173 remains healthy (HTTP 200). Preserved saved campaigns and all failed diagnostics. Heartbeat remains active until the original 21:45 UTC deadline; no training is running.
- 16:41 UTC heartbeat slice: **149 tests / 22 files pass**, build `index-CUXSpBbD.js`, rules `deterministic-7`. Reproduced a loaded carrier stuck despite a clear reversed pickup trace; reuse now validates every segment against current geometry and preserves cargo on failure. Withdrawal HUD now shows forward stocks and physical arrival counts, without changing saved state. The suspected cover bug was not confirmed (old readout correctly said 0/8 sheltered); this is contextual presentation repair. Aged 300-person browser checks pass at 1×/5× (p95 6.2 ms, full advancement); 1,000 at 5× **still fails** (68.5 simulation seconds in 20.26 wall seconds, p95 48.7 ms). A 45-second bounded long-return diagnostic stopped with the carrier still walking and cargo conserved, so complete delivery is not claimed. See `return-routes-bugpass-2026-09-23.md`. Next: profile the later 1,000-person state and returns without valid traces. No new systems or training.
- 16:54 UTC cleanup: closed owned QA browser `frontlines-return` and stopped verified preview PID 37128 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). All prior and incomplete evidence preserved; heartbeat remains active until the original deadline.
- 17:11 UTC heartbeat slice: **152 tests / 22 files pass**, build `index-2wMSJVWL.js`, rules `deterministic-8`. A replacement external route search exhausted its budget despite a clear final approach; now it can connect a fully collision-checked visible leg, with per-search terrain-cost memoization. No larger search cap or reduced collision checks. Carrier 947 completes the full return and physical delivery within the existing 45-second diagnostic budget, with conserved stock. Aged 300-person browser checks pass at 1×/5× (p95 6.2 ms, full advancement); 1,000 at 5× **still fails** (78.5 simulation seconds in 20.15 wall seconds, p95 54.7 ms). Real browser defense completes in defeat at 345.75 seconds / 2 able; result remains frozen under commands and the saved carrier campaign survives unchanged. See `visible-approach-bugpass-2026-09-23.md`, including rejected heuristic experiments, corrected obstruction diagnosis and repeated uncontended performance rows. Next: remaining outward-search failures, crowded delivery/fresh aging, and matched speed-continuation checks. No new systems or training.
- 17:26 UTC cleanup: closed owned QA browser `frontlines-search` and stopped verified preview PID 18556 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Original deadline and narrowed scope unchanged; all prior and failed evidence preserved.
- 17:41 UTC heartbeat slice: **158 tests / 23 files pass**, build `index-Bf_VZgy0.js`, rules `deterministic-9`. Reproduced fast-forward changing simulation outcomes, including a victory becoming a defeat; fixed by repeating the original tick rather than enlarging it. All three speeds now have identical normalized full-state hashes for three matched headless defense seeds, preserving prior 1× histories. Browser defense completes at 408.30 seconds in defeat, result remains frozen, and paused save restores exactly before/after stress checks. Scoped aged 300-person performance still passes (p95 6.2 ms); corrected-tick 1,000-person 5× is **worse and fails** (p95 315.9 ms, 61.5 simulation seconds / 20.246 wall seconds). Browser asynchronous navigation remains a separate determinism limitation. See `simulation-speed-bugpass-2026-09-23.md` for exact evidence, retained failed QA probe, and claim limits. Next: corrected-tick CPU profiling, matched worker timing, remaining outward routes and fresh aging. No training or major systems.
- 17:56 UTC cleanup: closed owned QA browser `frontlines-speed` and stopped verified preview PID 33204 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Saves and failed evidence are preserved; heartbeat remains active until the original deadline. No training started.
- 18:11 UTC heartbeat slice: **167 tests / 25 files pass**, build `index-z2DqhRxU.js`, rules remain `deterministic-9`. Profiled supply-queue checks repeatedly scanning distant camps; a conservative body/reservation broad phase reduces a five-second continuation from 1,193.71 ms to 439.26 ms with identical full state. Real browser testing also reproduced Load leaving restored troops off-screen; it now focuses a friendly's actual position without altering the save. Final aged 300-person checks pass at 1×/5× (p95 6.2 ms); 1,000-person 5× **still fails** (p95 115.4 ms, worst 741.2 ms, 69.5 simulation seconds / 20.1855 wall seconds). P95 improves but long hitches remain. See `queue-performance-bugpass-2026-09-23.md` for pre/post profiles, repeated final measurements and exact save checks. Next: later-state corrected-tick hitch profiling, worker timing, remaining routes and fresh aging. No major systems or training.
- 18:28 UTC cleanup: final real-control sandbox check reaches 215.85 simulation seconds with all 28 active and 7/7 watch present; forward stock exists and an empty shuttle is returning, but foot delivery is not yet completed in that snapshot. Original paused save restores exactly. Closed owned QA browser `frontlines-queue` and stopped verified preview PID 4480 on 4174; user Vite PID 35696 remains healthy (HTTP 200). All evidence retained; heartbeat remains active until the original stop boundary.
- 18:41 UTC heartbeat slice: **169 tests / 25 files pass**, build `index-BzB2S0bZ.js`, rules remain `deterministic-9`. Later-camp CPU profiling exposed another repeated full-camp distance scan at forward pickup berths; a conservative broad phase preserves the matched 600→700-second complete state and accounting. Browser 300-person 1×/5× checks pass (p95 6.2 ms). **1,000 still fails**: p95 127.6 ms, worst 473.8 ms, 80 simulation seconds / 20.0872 wall seconds; no precise overall ratio claimed. Continued actual sandbox play reaches 440.85 seconds, completes physical foot deliveries (cache 45 food / 51 water / 12 materials), all 28 active, balanced ledger. Offensive/defensive entry smoke checks preserve the original paused save exactly; no completed combat match claimed this slice. See `pickup-hitches-bugpass-2026-09-23.md`. Next: measured capacity rebuild/all-garrison rescheduling costs, repeated failed outward searches, fresh aging and browser-worker timing. No major systems or neural training.
- 18:53 UTC cleanup: closed owned QA browser `frontlines-hitches` and stopped verified preview PID 34620 on 4174. User Vite PID 35696 remains healthy (HTTP 200). All saves and evidence preserved. Heartbeat stays active within the original deadline; no training started.
- 19:11 UTC heartbeat slice: **173 tests / 26 files pass**, build `index-MgM8Ff_1.js`, rules remain `deterministic-9`. Profiled capacity-floor allocation reduced without changing geometry, arithmetic or the matched 600→700-second full state. Scoped 300-person browser checks pass at 1×/5× (p95 6.2 ms). **1,000 still fails**: p95 103.2 ms, worst 419.1 ms, 85 simulation seconds / 20.0738 wall seconds. Actual sandbox play completes rest and meal facilities, reaches 17 sleeping with 7/7 watch, and verifies a night Alert→Routine drill: 17/14 watch during the alert sample, then 7/7 with 16 sleeping. All 28 active, conserved inventory; ordinary Load restores the unchanged mid-construction save exactly. Final browser console has no errors/warnings. See `capacity-hitches-bugpass-2026-09-23.md`, including timing limits and preserved failed early artifact read. Next: all-garrison coordination, repeated outward failures, longer night/store construction checks, fresh aging and worker timing. No new systems or training.
- 19:25 UTC cleanup: closed owned QA browser `frontlines-capacity` and stopped verified preview PID 30216 on port 4174. User Vite PID 35696 remains healthy (HTTP 200); final production hash rechecked. Saves and all prior evidence preserved. Heartbeat remains active until the original 21:45 UTC deadline; no training started.
- 19:34–19:43 UTC direct user fix: names/labels glitching during camera motion. Refreshed camera matrices before projection, separated per-frame positions from throttled label content, and added immediate hiding plus a 150 ms post-settle delay / 120 ms fade. Hidden labels are inert; the fixed HUD stays visible and reduced motion disables the fade. **178 tests / 28 files pass**, production `index-BsUCQVTw.js`, rules unchanged. Actual browser pan/rotation/zoom/map/focus/selection and small-window checks pass; corrected timing evidence and retained throttled probes are in `camera-labels-bugpass-2026-09-23.md`. No simulation, save or training changes. Continue narrowed bug/polish work within the original deadline.
- Camera-label cleanup: closed owned QA session `frontlines-labels` and verified preview PID 27652 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Saves and evidence preserved; original heartbeat deadline unchanged.

- 19:44–20:15 UTC direct-user combat/visibility pass: explicit request adds more lethal/readable rifles, human spotting, nearby alarms, nearest completed trench entry and understandable trench command. Implemented under rules `deterministic-10`, save schema 2. **197 tests / 31 files pass**, production `index-COOcp4KX.js`. Ordinary browser offensive wins at 458.85 seconds / 44 friendly able; final-build night fixture wakes sleepers, kills eight exposed enemies through real fire, and returns to routine. Final browser checks preserve exact paused saves, hide enemy packs, retain stationary last-known contacts and fix the 920-pixel HUD overlap. See `combat-visibility-bugpass-2026-09-23.md` for build distinctions, full evidence and failed probes. No training, agents or external changes. Return subsequent heartbeat work to scoped bugs/playthroughs; preserve the original 21:45 UTC stop boundary.

- 20:16 UTC cleanup: final build and exact paused load rechecked, browser console clean. Closed only QA session `frontlines-combat` and verified preview PID 6204 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Saved campaigns and prior evidence preserved; no training running. Heartbeat deadline remains unchanged.

Latest direct-user addition, 20:17–20:36 UTC: **tactical enemy AI** implemented under rules `deterministic-11`, separate from neural learning and rendering. Enemy squads defend/capture, coordinate support and flanking, seek cover, withdraw under losses/low readiness, and physically resupply at finite captured caches. Hidden opposing positions/orders are excluded from planner observations; stable, validated plans survive saves. **213 tests / 32 files pass**, build `index-CkH9MaOF.js`. Six headless matches conserve stock; real-control browser defense ends in enemy village capture at 290.60 seconds, offensive victory at 461.10 seconds / 44 friendly able. Paused AI/travel save restores exactly across both modes, result remains frozen, console clean. See `enemy-ai-2026-09-23.md` for identities, retained QA failure and limits. No neural training, agents, publishing or external changes. Subsequent heartbeat work returns to bugs/playthroughs within the original 21:45 UTC stop boundary; difficulty/believability still need user review.

- 20:36 UTC cleanup: closed only owned QA browser `frontlines-enemy-ai` and verified preview PID 24424 on port 4174. User Vite PID 35696 remains on 4173, HTTP 200. Original saves, initial and revised scenario evidence, and failed probe source are preserved. No training running; heartbeat deadline unchanged.

Latest direct-user addition, 20:37–20:56 UTC: **middle-out, branching engineer work parties** under rules `deterministic-12`. Stable small crews start at the midpoint or a completed junction, dig both directions, split onto connected branches and rejoin unfinished fronts. Work requires physical arrival and fit unsuppressed workers; old excavation stays in place. Hold/Resume and validated save continuity cover all fronts. **226 tests / 33 files pass**, build `index-DTSKz6j5.js`. Real-control browser T-shaped earthworks complete at the 266.85-second sample with an empty queue; exact paused save restores, console clean. Six headless operation payloads match the prior AI pass and conserve inventory. See `engineer-work-parties-2026-09-23.md` for scope, identities, screenshots and retained probe failures. No training, agents or external changes. Subsequent heartbeat work returns to scoped bugs/playthroughs and the original 21:45 UTC deadline.

- 20:57 UTC cleanup: closed owned QA browser `frontlines-engineers` and verified preview PID 35676 on 4174. User Vite PID 35696 on 4173 remains healthy (HTTP 200). Final hash rechecked unchanged, exact paused save retained, console clean. All previous/failed evidence preserved; no training running. Original heartbeat deadline unchanged.

Latest direct-user correction, 20:57–21:02 UTC: **labels must stay visible, not hide during camera motion**. Removed the earlier visibility debounce and fade; retained current-camera matrices and per-frame positions, and initialized labels immediately. **226 tests / 33 files pass**, build `index-C207BX6I.js`, rules unchanged at `deterministic-12`. Actual browser pan/rotation/zoom/focus/small-window checks: 600/600 frames visible, 4,124 anchor checks within 0.000067 CSS pixels of the current projection. A world-label click changes squad selection while panning. Paused simulation and save remain unchanged, console clean. See `continuous-label-tracking-2026-09-23.md`. Prior hiding report is historical and must not drive future behavior. No training or simulation changes; original heartbeat scope/deadline unchanged.

- 21:02 UTC cleanup: closed owned `frontlines-label-tracking` browser and verified preview PID 4860 on 4174. User-facing 4173 remains healthy (HTTP 200); final hash unchanged, console clean, saves/evidence preserved. No training running; original deadline unchanged.

## Final scoped pass

- 21:03–21:37 UTC final scoped bug/playthrough slice: **241 tests / 36 files pass**, final production `index-8vGArMDy.js`, rules `deterministic-13`. Reproduced and fixed Resume choosing a distant job instead of the nearest left work face, engineer travel sliding/idle exertion, and direct reassignment between separate trenches being rejected. Explicit transfers now walk out/cross/enter, survive saved continuation, respect cancellation/withdrawal, and display Relocating. Ordinary disconnected recovery errands remain blocked. Actual browser T-shaped jobs finish; all ten reassigned riflemen enter, take watch, and receive physical shuttle/foot supplies with balanced inventory. Defense completes in defeat at 280.55 seconds; offensive victory at 456.50 seconds / 46 able. Both results remain frozen under commands; original mid-transfer QA save restores exactly. Six headless matches preserve earlier operation payloads/full states. See `engineer-transition-bugpass-2026-09-23.md` for artifacts, retained failed probes, hashes and limits. No training, agents, major systems, publishing or user-save edits. Stop deadline remains 21:45 UTC.

## Closeout

21:37 UTC cleanup: final build hash unchanged, browser console clean, both mode results verified frozen and the paused 441.30-second transfer save restored exactly. Closed only owned QA session `frontlines-engineer-edge` and verified preview PID 17956 on 4174. User Vite PID 35696 remains on 4173, HTTP 200. All development/test work is finished; waiting only for the original 21:45 UTC cutoff to pause the heartbeat. No training, extra helpers or new jobs started.

## Acceptance gates

Post-deadline direct-user screen-fit request, 22:54–23:06 UTC: repaired overlapping/cut-off HUD at narrow and short window sizes. Height-aware compact Force/Squad/Map drawers, measured content spacing, scrollable inspectors/menus and canvas-based renderer resizing; world labels remain continuously visible. **272 tests / 42 files pass**, production `index-DNxtrZuF.js`, SHA-256 `b92483e96a62d0656bf6608b6bba4805d536570763810b71cbd4fcb48b5af0ab`. Actual Edge controls and screenshots verify 15 viewport sizes (320×568 through 1920×1080), no collapsed-HUD overlap/overflow, and exact paused-state/save preservation. See `screen-fit-2026-09-23.md`. Closed only owned QA session `frontlines-screen-fit` and verified preview PID 18604 on port 4174; user Vite PID 35696 remains healthy on 4173. Rules unchanged at `deterministic-15`; no new training, agents, automation or user-save writes. Heartbeat stays paused.

Post-deadline direct-user request, 22:07–22:53 UTC: **open-ended Trench war campaign** with save/resume (the user's selected option), two prepared fronts and physical faction supply chains, limited enemy raids/returns, player-placed support/ammunition works, direction-aware dispersed watch, and shared trees/buildings/heightfield for environmental sight and fire. WWII primary references informed the abstractions; this is not full military simulation. Rules `deterministic-15`, **267 tests / 41 files pass**, build `index-Di8rhtlq.js` (SHA-256 `3233333274a3cd4d4aaedb9a9bc9248ff48f27653997792038f2670644300b3d`). A 72-campaign-hour seed remains active with balanced inventory and exact saved continuation; all six short-mode headless checks terminate, but combat balance is not certified. Real Edge controls verify placement, alarm, completed ammunition dugout/connector, mode transitions and exact restoration of the 869.70-second QA save. Normal 96-person combat and aged 300-person living fixtures meet p95 16.7 ms at 1×/5×; **aged 1,000-person 5× fails**, p95 170.2 ms and only 77.75 simulation seconds in 20 wall seconds. All failed/contaminated probes are retained and distinguished. See `trench-campaign-2026-09-23.md`. This is separately authorized interactive work, not a heartbeat restart. No agents, training, user-save edits, external publication or new automation.

Post-deadline direct-user request, 21:50–22:07 UTC: repaired engineer floor/cover and misleading occupancy counts; separate persistent Edge session opened. This is separately authorized interactive work, **not a heartbeat restart**. Rules `deterministic-14`, 250 tests / 37 files, production build and real-control Edge digging/completion/save-load checks. All eight workers have physical trench cover; labels distinguish inside from assigned. See `engineer-grounding-2026-09-23.md` for retained reproduction, final bundle identity, harness failure and limits. Existing in-app saves and prior evidence preserved; no training or agents. Heartbeat remains paused.

**21:45 UTC deadline reached:** heartbeat `frontlines-overnight-development` is **PAUSED**, confirmed by the app tool and saved configuration. Its prompt, cadence, name and task target were preserved. No further development, experiments or training continued beyond the window; development had already finished at 21:37. User-facing port 4173 still returns HTTP 200. Further work requires new user direction.

- Old v1/v2 saves still load; optional operation payload is validated and serializable.
- Peaceful sandbox behavior remains unchanged unless deliberately documented.
- Enemy squads cannot be selected or commanded by player controls.
- Pausing/menu/replay blocks simulation and command input; returning restores prior speed, not wall-clock catch-up.
- Objectives require physically present able troops; contested areas do not capture; victory/defeat freezes the result.
- Rifle fire requires a valid visible enemy and ammunition; cover changes combat outcomes; no damage through buildings.
- Finite caches and combat ammo participate in inventory accounting; no hidden replenishment.
- Evidence includes unit tests, build, a real browser screenshot and interaction checks. Believability remains the user's acceptance step.
