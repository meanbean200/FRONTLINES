async () => {
  throw Error('Retired: clearing CDP metrics does not reset a reusable Playwright context. Preserve the old profile and saves; open a normal Edge window. Run viewport QA in disposable browsers only.');
}
