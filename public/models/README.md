# Retired experimental models

The current game uses only deterministic coordination. No neural worker or ONNX runtime is imported into its browser bundle. No trained model was adopted or installed.

Earlier study scripts, source snapshots, checkpoints and failed runs are retained as historical evidence. They are not compatible with the current deterministic revision and must not be treated as a completed comparison. No further training or installation is scheduled.
