// Search bounds are cost limits, not guaranteed detection or weapon range.
export const SIGHT_RULES=Object.freeze({
  dayRange:800,nightRange:350,memorySeconds:18,
  recognitionSignal:.24,trackingSignal:.14,trackingSeconds:6,openTransmission:.65,
});
